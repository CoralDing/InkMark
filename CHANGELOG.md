<!--
文件说明：墨记版本变更记录，按照 Keep a Changelog 风格维护用户可感知变化。
作者：Codex
创建时间：2026-07-29
-->

# 变更日志

## 1.0.1 - 2026-08-06

### 改进

- 发布流程现在使用 `jlink` 裁剪 PlantUML 所需的 Java 运行时，并分别为 Apple Silicon 与 Intel 生成最小依赖。
- 保持 PlantUML 完全本地、离线可用的同时，显著减少三种 macOS 安装包中 Java 运行时带来的体积占用。
- 发布前会验证精简运行时的必要模块与预览图表渲染路径，避免安装包因运行时缺失而无法展示 UML 图表。

## 1.0.0 - 2026-08-05

### 修复

- 修复分栏模式中编辑区与预览区滚动后章节对应偏移的问题。
- 修复 Mermaid、PlantUML、数学公式和目录异步完成渲染后预览位置跳动的问题。
- 文档顶部与底部现在严格同步到另一侧的首尾位置。
- 修复目录插件的平滑动画干扰原生滚动同步，导致图表较多时偶发章节偏移的问题。
- 设置页面改为从顶部向下排列，修复短页面垂直居中和分组卡片被拉伸的问题。
- 设置窗口固定内容区高度，较长分类改在右侧页面内部滚动。

### 改进

- 滚动同步改为按顶层 Markdown 源行锚点插值，复杂表格、引用和分栏内容不再干扰章节定位。
- 增加双向映射、锚点单调性和异步布局变化的真实预览运行时测试。
- 重新设计应用图标，以纸张、墨色 Markdown 标记和笔尖细节强化“墨记”的产品识别度。
- 增加 GitHub Release 自动发布流程，版本标签可触发双架构 DMG 构建和产物上传。
- Release 同时提供 universal、arm64 和 x86_64 三种 DMG，并为每种架构生成独立校验文件。

## 0.2.5 - 2026-08-05

### 修复

- 修复关闭插件总开关后 Mermaid 和数学公式退回源码展示的问题。
- Mermaid、PlantUML 与 KaTeX 改为始终可用的核心 Markdown 预览能力。

### 改进

- PlantUML 改用随应用分发的 Temurin JRE 在本机生成 SVG，不再上传图表源码。
- PlantUML 默认使用内置 Smetana 布局引擎，类图无需额外安装 Graphviz。
- 增加 PlantUML 渲染缓存、输入大小限制、实时输入延迟和 SVG 安全过滤。
- 插件总开关现在只影响文档目录、代码复制与用户安装脚本。

## 0.2.4 - 2026-08-05

### 新增

- “插入”菜单按文本、基础、布局和研发重新分组，新增可直接编辑的扩展 Markdown 模板。
- 新增 GitHub 提示块、响应式内容分栏和折叠块预览。
- 新增附件、视频、技术资产和 ZERO 文件资源卡片。
- 本地集成 KaTeX，支持行内、块级和 `math` 围栏数学公式。

### 改进

- 新增扩展语法着色，并让所有新块级组件保留双向滚动同步锚点。
- 资源地址限制为相对路径、文件、数据或 HTTP(S) 协议，拒绝脚本协议。

## 0.2.3 - 2026-08-04

### 改进

- 新增分栏模式双向滚动同步，按 Markdown 源行锚点匹配编辑内容与预览位置。
- 新增本地 Mermaid 图表渲染，以及默认关闭并带隐私说明的 PlantUML 远程渲染插件。
- 主窗口重构为写作、分栏和阅读三种经典模式，工具栏收敛为高频格式与模式切换。
- 编辑区增加 Markdown 语法着色、动态正文宽度，并与 Git、夜间、纸张等预览主题保持一致。
- 实时预览刷新时保留阅读位置，分栏模式恢复用户上一次的左右比例。
- 文档目录内的相对图片现在可在安全边界内正常显示，并通过修改时间缓存减少重复读取。
- 空文档取消品牌展示页，回归安静、内容优先的编辑器界面。

### 修复

- 连续多行引用和引用内空行现在保持在同一个引用容器中。
- 支持使用 `~~~` 的围栏代码块，并正确匹配围栏符号与长度。
- 修复窄窗口表格状态列过度换行，以及“隐藏其他”快捷键冲突。
- 加强链接与图片属性边界转义，避免特殊引号破坏预览结构。

### 工程

- 发布包标识更新为 `io.github.dingyi60.moji`，移除临时 `com.local` 标识。
- Markdown 类型声明改为导入系统类型，并将规范链接升级为 HTTPS（加密网页协议）。
- 增加 Markdown 渲染冒烟测试，并接入 GitHub Actions。

## 0.2.2 - 2026-07-29

### 新增

- 增加可直接安装的阅读时间插件示例。
- 增加真实应用截图、开源发布说明和一键 DMG 打包脚本。

### 改进

- 设置窗口默认进入通用页，并记住用户调整后的尺寸和位置。
- 统一系统快捷键展示格式，补充技术架构与 MIT 开源许可信息。
- 清理旧工程、历史依赖、本地构建缓存和过时发布产物。

### 验证

- 实机检查菜单、设置、表格、任务列表、目录与代码复制功能。
- 发布产物同时包含 Apple Silicon 与 Intel 架构，并执行签名和磁盘映像校验。

## 0.2.1 - 2026-07-29

### 新增

- 内置文档目录与代码复制插件。
- 用户 `.js` 插件安装、扫描、单独启停和访达定位。
- 设置页启动时新建空白文档选项。

### 改进

- 重构设置窗口层级、侧栏、分组、间距和关于页。
- 优化编辑器行距、预览响应式边距、代码块和空文档状态。
- 支持相对图片、外部链接和页内锚点。
- 同时支持 Apple Silicon 和 Intel 架构。

### 安全

- 插件改用 `WKUserScript` 注入。
- 增加预览 CSP，阻止脚本主动建立网络连接。
- 移除任意网络访问、旧更新源、AppleScript 和过宽文件类型声明。
